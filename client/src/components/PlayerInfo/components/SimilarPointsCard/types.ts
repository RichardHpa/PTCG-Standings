import type { Standing } from 'types/standing';
import type { Division } from 'types/divisions';

export interface SimilarPointsCardProps {
  player: Standing;
  playerIndex: number;
  division: Division;
}
